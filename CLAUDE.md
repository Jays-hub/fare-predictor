# CLAUDE.md — ATL Domestic Fare-Drop Predictor

Context for any AI assistant or collaborator picking up this project.

## Goal & Scope
- Personal **buy-vs-wait recommender + mistake-fare alarm** for flights out of ATL.
- **Success criterion (sharpened 2026-06-12): beat Google's own price verdict at its own job.** A model policy must beat `google_says_low` (and every naive baseline) in **test-split harness dollars**. Google's `price_level` is per-market; ours is per-carrier — the improvement claim is **granularity and accuracy**.
- Scope: **roundtrip domestic flights from ATL, Delta and Frontier only.**
- Edge = **modeling depth on a narrow dataset**, not data breadth. ML-first.
- Budget: ~$0–5/month. Effort: ~1 week plumbing, rest on DS/ML.
- Current watch list: routes **MCO, LAS, DEN**; date-pair ladder (3-day = Fri→Mon, 7-day = Mon→Mon): **06-26→06-29**, **07-10→07-13**, **07-31→08-03** (3-day) and **07-20→07-27**, **08-17→08-24** (7-day, all 2026); economy, 1 adult.

## Three governing principles
1. **Data is time — you can't backfill it.** The collector runs first; model quality is gated by weeks collected.
2. **Judge models in dollars saved vs. "buy now," not RMSE.** Build the eval harness before any model.
3. **Scope is the edge.** Delta & Frontier compete head-to-head on ATL leisure routes, so each carrier's price predicts the other's. Protect that signal.

## Data Source (`fast-flights`) — hard-won facts
- Parses Google Flights' internal protobuf via `primp`; no official API.
- **No airline filter at code level** — `FlightData` takes only `date`/`from_airport`/`to_airport`/`max_stops`. Filter to carriers **client-side** by the `name` field.
- A **round-trip query returns only OUTBOUND flights**, each tagged with the **round-trip TOTAL price** (confirmed against the live UI).
- `Result` has `current_price` (Google's low/typical/high verdict) + `flights`.
- **Best flights are listed twice** (best section + all-flights section) → must dedup.
- `primp` chrome impersonation falls back to "random" (profiles not compiled in 1.3.1 on M-series Mac). Does **not** corrupt data — only lowers hit rate. Bot-detection blocking is **probabilistic/intermittent**.
- Degraded responses happen: prices parse but `name`/times come back empty → unusable (can't attribute carrier) → treat as soft failure and retry.
- Library's own `fetch_mode` fallback chain hits a **Turnstile 401** — do NOT use it; our retry logic replaces it. Use default `common` mode.

## Architecture & Design Decisions
- **Reliability layer** (`fetch_with_retry`): wraps `get_flights`; retries on **any raised exception** (known cases: RuntimeError "Loading results" stub, AssertionError non-200) AND soft degraded-parse; 4 attempts, exponential backoff + jitter; returns `None` on total failure (never raises) so the loop continues.
- **Second pass**: combos that fail the first pass retried once after a 30–60s cooldown to decorrelate from transient blocking.
- **GitHub runner > local Mac** for collection — x86 `primp` impersonation works there, giving higher success than the M-series laptop.
- **Storage = parallel CSV + Postgres.** CSV always written (local inspectability); Postgres written only when `DATABASE_URL` is set (env-var toggle, no flags). Neon serverless Postgres. **`ON CONFLICT DO NOTHING` + unique index** for write-time dedup.
- **Postgres is the source of truth**; the local CSV only holds rows from laptop runs (runner CSV is ephemeral). Phase 2 EDA reads from Neon. CSV→Parquet migration deferred.
- **Time handling**: `observed_at` stored UTC ISO; **one shared timestamp per collection cycle** (gives each cycle a clean snapshot identity). `days_to_dep` computed on the **UTC date** — equals the Eastern date at the 10:00/22:00 UTC snapshot times, so **no `tzdata` dependency**.
- **Store precision, derive buckets.** `dep_hour`/`dep_minute` stored exact; time-of-day buckets (redeye, pre-noon) derived at modeling time so definitions can change without recollecting. Never round/bucket in storage.
- **Date-pairs are FIXED calendar anchors**, NOT regenerated from `today()` each run — otherwise the departure date drifts daily and you never observe the same itinerary twice (breaks trajectory reconstruction).
- **Horizon ladder**: keep 3–5 staggered anchors per trip shape (consistent day-of-week shapes: Fri→Mon, Mon→Mon) so the panel always spans a spread of days-to-departure. Expired anchors are **skipped automatically** at run time (`live_date_pairs`); the freshness monitor **hard-fails when the farthest collected anchor is <14 days out** (soft-warns <21), so running out of horizon produces an email, not silent decay. Each ladder commit also resets GitHub's 60-day workflow auto-disable clock.
- **`price_level` captured per query**: Google's low/typical/high `current_price` verdict is denormalized onto every row (nullable). It can't be backfilled, and it serves as both a must-beat baseline ("buy iff Google says low") and a model feature. The collector self-applies the column migration (`ADD COLUMN IF NOT EXISTS`) so schema and code can't race; canonical DDL lives in `db/schema.sql`.
- **Two trajectory views** (`trajectories.py`): `carrier_min_series` (per-carrier cheapest fare per snapshot = headline buy-vs-wait unit, immune to flight reordering) and `departure_series` (per-departure, `flight_key` tracks one flight over time, for time-of-day modeling).
- **IO separated from transformation**: only `load_snapshots` touches the DB; all reshaping functions take/return DataFrames (testable on synthetic data, reusable by the harness).
- **Reusable module > notebook** for shared logic (harness + notebooks import it; modules diff cleanly in git).
- EDA delivered as a **percent-script (`# %%`)**, not `.ipynb` — git-friendly, runs as a notebook in VSCode.

## Conventions & Rules
- **Run all commands from the REPO ROOT** (`pip freeze`, `git`, `python scripts/collect.py`), never from inside `scripts/`. Code anchors paths via `Path(__file__)`. (Two stray artifacts — `scripts/data/` and `scripts/requirements.txt` — came from violating this.)
- **`requirements.txt`: root is the single source of truth** (the workflow installs from it); keep it **fully pinned** via `pip freeze`. No bare package names.
- **Secrets**: `DATABASE_URL` NEVER in code / repo / workflow YAML — only in a local shell `export` and **GitHub Actions secrets**. The repo is **PUBLIC**. (Connection string was leaked in chat twice and rotated — assume any string pasted into chat is burned.)
- **Public code, private data**: code is public; collected data lives only in Neon, never committed — matches the "don't redistribute the data" boundary.
- **Stay at hobby scale**: no proxies / CAPTCHA evasion. Needing them is the signal you've outgrown hobby scale, not a cue to escalate.
- **Logging discipline**: every `save_to_postgres` outcome logs one of `wrote N rows (M duplicate(s) skipped)` (real insert count via `cur.rowcount`, not attempted count) / `write failed` / `CSV only`; each cycle logs total rows + unrecoverable count + any skipped expired date-pairs.

## What's Been Built
- `scripts/validate_source.py` — Phase 0 source validation (kept as a record).
- `scripts/collect.py` — production collector: watch list (routes × date-pair ladder, expired pairs auto-skipped), `fetch_with_retry`, `parse_result` (dedup, price/duration/departure parsing, carrier filter, `price_level` stamp, schema rows), `persist` (CSV + Postgres incl. self-applied micro-migration), `run_once` (first pass + cooldown second pass).
- `scripts/trajectories.py` — reconstruction module: `load_snapshots`, `carrier_min_series`, `departure_series`, `pivot_min_by_carrier`, `itinerary_id`.
- `scripts/harness.py` — evaluation harness (principle #2 made executable): `simulate`/`summarize`/`evaluate` (causal buy/wait per itinerary vs. buy-now & oracle, dollar-weighted), `split_by_dep_date` (time-aware), and naive baseline policies. IO-free — consumes a `carrier_min_series` frame; the CLI `__main__` prints a real-data baseline leaderboard.
- `scripts/check_deals.py` — mistake-fare alarm: pure `find_deals()` (≥20% AND ≥$20 below 14-day trailing min, ≥3 prior snapshots; judges each itinerary's OWN latest snapshot if within an 18h freshness cap, so a partly-blocked cycle doesn't blind it) + `check()` that exits 1 on a deal to fire the email (inverted fail→email semantics).
- `scripts/features.py` — `build_features()`: causal model features on `carrier_min_series` (one row in/out). Own-trajectory (lag/delta/pct, trailing-min & distance, rolling min/mean/std), fare-steps (up-step flag/count/size — thesis C), `days_min_held`, and the **cross-carrier edge** (competitor's same-snapshot fare + gap/ratio — principle #3). Strictly no look-ahead so the harness split stays leak-free; **labels (forward-looking) are NOT built here** — they belong to the model step. Holiday flags deferred (calendar, not trajectory). Real panel: 100% competitor coverage.
- `tests/` — pytest suite (38 tests) for the parsing layer, reshaping logic, harness dollar-accounting, deal detection, and feature causality — all on synthetic data (no network/DB); `.github/workflows/test.yml` runs it on every push.
- `notebooks/eda_thesis_check.py` — thesis-validation EDA (percent-script); dual-mode: shows plots inline in VSCode, or with `EDA_SAVE_FIGS=1` saves them to `notebooks/figures/` (gitignored) for a headless run. **Run on real data 2026-06-11 — all 3 theses held** (see What's Next).
- `scripts/check_freshness.py` — staleness check (hard fail at >24h) + horizon check (hard fail <14 days, warn <21) + route/carrier-coverage warnings (soft).
- `.github/workflows/collect.yml` — scheduled collector at **10:00 & 22:00 UTC** (06:00/18:00 EDT, 05:00/17:00 EST — the UTC-date `days_to_dep` logic holds in both), plus `workflow_dispatch`; `DATABASE_URL` injected from secrets.
- `.github/workflows/monitor.yml` — freshness monitor ~3h after each collection; a failed run → GitHub failure email (silence = healthy).
- `.github/workflows/deals.yml` — mistake-fare alarm, triggered by `workflow_run` on `collect-fares` completion (NOT a fixed cron — Actions delays scheduled jobs 1–4h, so a guessed offset fires before collection lands); a failed run → email, but here failure = **deal found** (inverted vs. the freshness monitor).
- **Neon Postgres `snapshots` table** with dedup unique index; DDL recorded in `db/schema.sql` (reconstructed — verify the index definition against the live DB when convenient).
- **Status**: collecting since 2026-06-04 (~2.5k rows as of 06-11); scheduled-run DB writes verified at both log and database level. Pipeline is unattended, self-guarding (expiry skip + horizon alarm), and healthy. **EDA validated 2026-06-11; eval harness + mistake-fare alarm built and run on real data 2026-06-12 (baselines top out ~55% of oracle; alarm clean). Model framing decided 2026-06-12 — two LightGBM heads (P(drop ≥ $20 in 7d) classifier + forward-min quantile; sentiment = display layer; promotion = harness dollars). Next build steps: `scripts/labels.py` → Google scorecard → `model.py` (see What's Next).**

## What's Next / Open Questions
- **✅ DONE (2026-06-11) — EDA run on real data (~2.5k rows, 15 snapshots, 06-04→06-10).** All three theses survived: **(A) prices move** — per-itinerary swings up to 58% in 6 days, most fully-tracked itineraries >20%; **(B) Delta↔Frontier co-move** — every *defined* day-over-day change-correlation positive, two strong (LAS 07-20 +0.98, MCO 06-26 +0.86), noisy at n=14 but right sign; **(C) upward fare steps** — 23 steps, median $10, max $74. Verdict: premise is sound, building the harness/alarm on top is justified. Re-run weekly as the panel deepens. (`DATABASE_URL` via gitignored local `.env`; `EDA_SAVE_FIGS=1 python notebooks/eda_thesis_check.py` runs it headless, figures → gitignored `notebooks/figures/`.)
  - **`price_level` caveat**: only the latest snapshot onward has it (column shipped 06-10 eve; earlier 85% null is an un-backfillable migration artifact, not a parse bug — healthy going forward). Stored verdicts so far are only `typical`/`high` — **zero `low` yet**, so the planned **buy-iff-Google-says-low** baseline can't fire until a `low` is observed (an all-"wait" result there is expected, not broken).
- **✅ DONE (2026-06-12) — evaluation harness (`scripts/harness.py`).** Causal buy/wait simulation at every snapshot of `carrier_min_series`; each itinerary bracketed by **always-buy-now** and **oracle**; reports **$ saved vs. buy-now** and **% of oracle captured** (dollar-weighted, well-defined even when some itineraries had no room to save). `split_by_dep_date()` gives the time-aware split (itinerary key contains dep_date, so snapshots never leak across train/test). Causality is enforced by construction — a policy only ever sees the history-prefix up to "now"; waiting to the deadline forces a buy at the last snapshot. Ships with naive baselines (always-buy-now, deadline, fixed-days-out, below-trailing-median, **buy-iff-Google-says-low**). `price_level` is now threaded through `carrier_min_series` (per-query verdict carried via `first`), so the Google-low baseline is wired — but with **zero `low` observed yet it degrades to a deadline buy** (identical numbers; expected, not a bug). Latest real-panel run (30 itineraries, $1119 oracle headroom): `dip_5pct_below_median` led at **60% ($668)**, `deadline_buy`/`google_says_low` 59% — the dollar bar the first model must clear.
- **✅ DONE (2026-06-12) — mistake-fare alarm (`scripts/check_deals.py` + `.github/workflows/deals.yml`).** Pure `find_deals()` detector: a live fare ≥**20% AND ≥$20** below its **14-day trailing min**, requiring ≥3 prior snapshots (no baseline ⇒ stay quiet). Judges **each itinerary's own latest snapshot** (not one global cycle) when it's within an **18h freshness cap** — so a partly-blocked collection (the 06-11 cycle landed just 1 of 15 combos) doesn't blind the alarm to the itineraries that missed it, while genuinely stale prices are suppressed. Reuses the fail→email channel with **inverted semantics** (exit 1 = deal found = email; silence = healthy) — documented in-file so the non-zero exit isn't "fixed." Triggered by `workflow_run` on collection completion (cron offsets fire before the 1–4h-delayed collection lands). First real-panel run: 30 itineraries tracked, 0 deals, exit 0.
- **Next — model step, framing DECIDED 2026-06-12 (two heads, one feature set, judged in dollars):**
  - **Primary head — binary classifier: P(carrier-min fare drops ≥ $δ within H days).** Defaults **δ=$20** (mirrors the alarm's `MIN_DROP_DOLLARS`), **H=7d**; sanity grid H∈{3,7} × δ∈{10,20}. Explicitly **NOT** low/typical/high classes — mimicking Google re-learns their answer, invented class boundaries are arbitrary, and the harness can only score buy/wait. **Sentiment is a display layer derived from the two heads, not a trained target.** A short-H binary classifier *is* a discrete-time hazard model, so the survival/hazard step below is a graduation of this, not a rewrite.
  - **Secondary head — quantile regression of the forward-window min** (q10/q50, LightGBM `objective="quantile"`): magnitude for the decision margin + display. Point-regression of a future MIN is the wrong tool (extreme-value quantity). "Min until departure" is deferred — right-censored for every live itinerary (zero completed yet; first when 06-26 departs).
  - **Labels (`scripts/labels.py`, IO-free, no new deps): `will_drop(δ,H)`, `fwd_min(H)`, `label_observed`.** Censoring rules: a window is observed iff it can no longer change — fully elapsed (window end ≤ panel end) or itinerary departed; train BOTH heads only on observed rows. **Never impute unobserved as "no drop"** (floods the panel's right edge with false negatives → biases toward "buy"); don't keep early-confirmed positives from partial windows either (keeping determined-1s while dropping undetermined-0s over-samples positives at the same edge).
  - **Build order: `labels.py` → Google scorecard → `model.py`.** Scorecard = conditional drop rates by `price_level` verdict — the "are we actually beating Google" accuracy ledger (reuses labels; starts degenerate at zero observed "low" — expected). `model.py` = LightGBM wrapper + harness adapter + leaderboard.
  - **Model choice DECIDED: LightGBM** — native NaN (blocked-cycle gaps, un-backfillable `price_level`), native categoricals, quantile objective for free, **monotone constraints** as a small-data regularizer (P(drop) non-decreasing in `vs_trailing_min` and `price_ratio_competitor`). **Plus a regularized logistic guard on the same features — if trees can't beat logistic, ship the logistic.** Rejected: XGBoost (redundant), CatBoost (categorical edge wasted on 2 carriers × 3 dests), random forest (no native quantile), GP/BSTS (over-engineered at ~15 points/series), deep nets (still gated at ~10× panel). Survival/hazard on fare-step events remains the advanced step — same label viewed properly.
  - **Decision layer: wait iff P(drop) > p\*, with p\* tuned on train-split harness dollars** (the harness is the calibrator; skip isotonic at this n). Small-data posture: depth ≤ 4 / ≤ 15 leaves, `min_child_samples` ≈ 20, sub/colsample < 1, early stopping on an itinerary-grouped inner split. **AUC/Brier/pinball are dev metrics only — promotion is harness dollars (principle #2).**
  - New deps land only with `model.py`: `lightgbm` + `scikit-learn`, pinned in root `requirements.txt`.
- **Feature roadmap (assessed 2026-06-12).** Fix at the model step: **`FEATURE_COLUMNS` has no identity features** — add categoricals `carrier`, `dest`, trip shape (3d/7d), snapshot hour (10/22 UTC) + snapshot DOW, and the already-collected `n_flights`/`nonstop_available`. New families by thesis alignment: **(1) lead–lag competitor** (competitor delta/up-step/vs-trailing-min — thesis B's *predictive* form; today only the same-snapshot level is used), **(2) spread dynamics** (gap delta, gap z-score vs rolling mean/std — pairs-trading view of principle #3), **(3) carrier sale breadth** (share of the carrier's OTHER tracked itineraries dropping this snapshot — Frontier flash-sale detector; route version too; same-snapshot ⇒ causal), **(4) AP-fence crossings** (just crossed 21/14/7 days-to-dep — thesis C's mechanism), **(5) own-trajectory completions** (down-step mirrors, `vs_first_price`, trailing max/range position, hours since prev snapshot, snapshots since last up-step), **(6) `price_level` ordinal** {low 0, typical 1, high 2}, NaN preserved; later a no-price_level ablation to separate our edge from Google's. **Anti-features**: external data (holiday APIs, oil, events, Trends), seat-map scraping — off-scope/off-budget; holiday flags stay deferred (collinear with 5 fixed date-pairs). Budget ≈ 30–35 features at n≈300 labeled rows — families 1–3 + categoricals first.
- **Data maturity**: meaningful EDA ~3–4 weeks; trustworthy first model ~6–8 weeks (at ~1.5 now — build the pipeline, don't trust week-2 dollars). Cannot be compressed. **Current leaderboard numbers are provisional**: no itinerary has completed, so oracle math runs on truncated windows and shifts as they close. Once an anchor departs (first: 06-26), its itineraries are fully uncensored — **accumulate completed itineraries as a frozen backtest vault**.
- **Maintenance watch**: `fast-flights` may break when Google changes the endpoint (`pip install -U` / patch); GitHub auto-disables scheduled workflows after 60 days with no commits (ladder commits double as keep-alives); horizon refills are email-driven (monitor hard-fails <14 days) — when the email arrives, extend `DATE_PAIRS` keeping the Fri→Mon / Mon→Mon shapes; verify `db/schema.sql`'s unique-index definition against the live DB (`psql "$DATABASE_URL" -c '\d snapshots'`) and correct the file if it differs.